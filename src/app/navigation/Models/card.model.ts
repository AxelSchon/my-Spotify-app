export class cardModel{

img: string;
artistName: string;
albumName: string;

    constructor(img: string, artistName: string, albumName: string ) { 
        this.img = img;
        this.artistName = artistName;
        this.albumName = albumName;
      }
}